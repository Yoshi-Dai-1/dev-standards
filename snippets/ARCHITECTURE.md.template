# ARCHITECTURE.md

<!-- このファイルはプロジェクトルートに置く。AIへの指示時に渡す最重要文書。 -->
<!-- 各セクションの [] を実際の内容に書き換える。 -->

## プロジェクト概要

**目的**：[このプロジェクトが何をするものか1〜2文で]  
**種別**：[Webフロントエンド / バックエンドAPI / モノリポ / データパイプライン / ドキュメント]  
**採用アーキテクチャ**：[FSD / レイヤードアーキテクチャ / etc.]  
**参照ドキュメント**：[dev-standards の該当ファイルへのリンク]

---

## 技術スタック

| 種別 | 採用技術 |
|------|---------|
| フレームワーク | [React + Vite / Next.js / Express / FastAPI / etc.] |
| 言語 | [TypeScript / Python / etc.] |
| スタイリング | [CSS Modules / Tailwind / etc.] |
| テスト | [Vitest / Jest / pytest / etc.] |
| データ取得 | [SWR / React Query / axios / etc.] |

---

## 層のルール

<!-- アーキテクチャに合わせて書き換える -->

### 依存の方向（変更禁止）

```
[層A] → [層B] → [層C] → [層D]
```

上から下への依存のみ許可。逆方向は禁止。

### 各層の責務と禁止事項

| 層 | 責務 | 禁止事項 |
|----|------|---------|
| [層A] | [何をする層か] | [何をしてはいけないか] |
| [層B] | [何をする層か] | [何をしてはいけないか] |

---

## 命名規則

`dev-standards/principles/naming-conventions.md` に従う。
プロジェクト固有の追加ルール：

- [プロジェクト固有のルールがあれば記載]

---

## 禁止パターン

- [ ] [具体的な禁止事項1]
- [ ] [具体的な禁止事項2]
- [ ] shared/ が features/ に依存する（共通原則）
- [ ] 循環依存（import/no-cycle で自動検出）

---

## ファイルサイズ

- 通常のソースファイル：200〜300行以内
- 超えたときは責務を分割する

---

## 環境変数

`.env.example` を参照。環境変数は `config/env.ts`（または `config/settings.py`）で一元管理する。

---

## 決定事項のログ

| 日付 | 決定内容 | 理由の詳細 |
|------|---------|-----------|
| [日付] | [何を決めたか] | decisions/[番号]-[slug].md |
